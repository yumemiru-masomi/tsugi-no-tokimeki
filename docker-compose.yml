services:
  web:
    build:
      context: ./web
      dockerfile: dockerfile
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
    restart: unless-stopped
